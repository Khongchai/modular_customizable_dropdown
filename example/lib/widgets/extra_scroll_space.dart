import "package:flutter/material.dart";

///Extra scroll space to test top-down wrapping
class ExtraScrollSpace extends StatelessWidget {
  const ExtraScrollSpace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 400);
  }
}
