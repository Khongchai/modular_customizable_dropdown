class DropdownAlignment {
  final double x;

  const DropdownAlignment(this.x);

  static const DropdownAlignment left = DropdownAlignment(-1);
  static const DropdownAlignment center = DropdownAlignment(0);
  static const DropdownAlignment right = DropdownAlignment(1);
}
